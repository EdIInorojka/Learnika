# Wave 5 Slice 6 implementation note

## Slice

`WAVE 5 / SLICE 6 - DIAGNOSTIC SEPARATION-OF-DUTIES ENFORCEMENT POLICY PLACEHOLDER`

## Status

Static contract-data validation foundation only. This slice adds one global,
unresolved separation-of-duties enforcement policy placeholder. It does not
approve a policy, compare identities, evaluate an assignment, detect a
violation, grant an exception or activate runtime authorization.

## Added foundation

Slice 6 adds:

- the separation-of-duties policy placeholder contract;
- one versioned global placeholder artifact;
- one dependency-free deterministic validator;
- focused artifact, negative and exact-scope tests;
- this implementation note.

The artifact pins the Slice 2 activation prerequisites, Slice 5 reviewer role
ownership policy and Wave 4 authority and workflow placeholders. It references
the exact seven Wave 4 role placeholders and three non-authorizing separation
requirement groups without creating an eighth reviewer role or an executable
rule.

## Preserved prerequisite and product boundary

- `separation_of_duties_enforcement` remains `UNSATISFIED_DEFERRED`;
- its owner remains `UNASSIGNED_OWNER_PLACEHOLDER`;
- its evidence references remain empty;
- all nine policy requirements remain `TO_BE_DECIDED`;
- the policy remains `UNRESOLVED_DEFERRED`;
- enforcement authority remains unassigned and runtime enforcement disabled;
- no waiver or exception can authorize a decision or production approval;
- activation remains `BLOCKED` and workflow remains `INACTIVE`;
- readiness remains `NOT_READY` with exactly `INCOMPLETE_COVERAGE` and
  `NON_PRODUCTION_FIXTURES`;
- identities, assignments, conflicts, violations, waivers, exceptions,
  decisions and production approvals remain zero;
- all protected record arrays remain empty.

## Exact scope maintenance

The skill-graph guard adds only the two exact Slice 6 documentation paths. The
Wave 4 and Wave 5 upstream governance guards add only the five exact Slice 6
product files. The Slice 5 guard and new validator enumerate the complete
28-path local implementation set and admit no broad directory prefix.

Focused tests retain rejection of near-miss Wave 5, API, OpenAPI, Prisma, web,
runtime, workspace-manifest and lockfile paths and verify the root test-command
registrations.

## Excluded implementation

No dependency, lockfile, Prisma schema, migration, API, OpenAPI, web, runtime,
provider, diagnostic candidate, student-data, analytics or deployment change
is part of this slice. Upstream artifacts remain unchanged.

## Validation and rollback

The complete validation chain from the approved prompt is required and is
reported in the Slice 6 handoff. Green validation is not policy approval,
identity evidence, assignment evidence, separation proof, violation evidence,
exception authority, activation evidence or CI evidence.

Before commit, rollback is deletion of the five new Slice 6 files and reversion
of their exact guard, test and `package.json` registrations. No database, API or
runtime state requires rollback.

## Handoff boundary

Approval of Slice 6 approves only this unresolved placeholder foundation.
Slice 7 and any prerequisite satisfaction, real identity, assignment, conflict,
violation, waiver, decision, approval, activation or readiness change require
separate user authorization.
