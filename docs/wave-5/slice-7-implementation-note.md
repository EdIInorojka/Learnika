# Wave 5 Slice 7 implementation note

## Slice

`WAVE 5 / SLICE 7 - DIAGNOSTIC CONFLICT-OF-INTEREST POLICY PLACEHOLDER`

## Status

Static contract-data validation foundation only. This slice adds one global,
unresolved conflict-of-interest policy placeholder. It does not approve a
policy, collect a disclosure, evaluate a relationship, recuse or reassign a
reviewer, grant an exception or activate runtime authorization.

## Added foundation

Slice 7 adds:

- the conflict-of-interest policy placeholder contract;
- one versioned global placeholder artifact;
- one dependency-free deterministic validator;
- focused artifact, negative and exact-scope tests;
- this implementation note.

The artifact pins the Slice 2 activation prerequisites, Slice 5 reviewer role
ownership policy, Slice 6 separation-of-duties policy and Wave 4 authority and
workflow placeholders. It defines only unresolved category and policy
structures and creates no real relationship, identity or assignment.

## Preserved prerequisite and product boundary

- `conflict_of_interest_policy` remains `UNSATISFIED_DEFERRED`;
- its owner remains `UNASSIGNED_OWNER_PLACEHOLDER`;
- its evidence references remain empty;
- all ten policy requirements remain `TO_BE_DECIDED`;
- the policy remains `UNRESOLVED_DEFERRED`;
- escalation authority remains unassigned and runtime evaluation disabled;
- no waiver or exception can authorize a decision or production approval;
- activation remains `BLOCKED` and workflow remains `INACTIVE`;
- readiness remains `NOT_READY` with exactly `INCOMPLETE_COVERAGE` and
  `NON_PRODUCTION_FIXTURES`;
- identities, assignments, relationships, conflicts, disclosures, recusals,
  waivers, exceptions, audit events, decisions and approvals remain zero;
- all protected record arrays remain empty.

## Exact scope maintenance

The skill-graph guard adds only the two exact Slice 7 documentation paths. The
Wave 4 and Wave 5 upstream governance guards add only the five exact Slice 7
product files. The Slice 5 and Slice 6 guards plus the new validator enumerate
the complete exact local implementation set and admit no broad directory
prefix.

Focused tests retain rejection of near-miss Wave 5, API, OpenAPI, Prisma, web,
runtime, workspace-manifest and lockfile paths and verify the root test-command
registrations.

## Excluded implementation

No dependency, lockfile, Prisma schema, migration, API, OpenAPI, web, runtime,
provider, diagnostic candidate, student-data, analytics or deployment change
is part of this slice. Upstream artifacts remain unchanged.

## Validation and rollback

The complete validation chain from the approved prompt is required and is
reported in the Slice 7 handoff. Green validation is not policy approval,
identity evidence, assignment evidence, disclosure evidence, conflict-free
evidence, recusal or exception authority, activation evidence or CI evidence.

Before commit, rollback is deletion of the five new Slice 7 files and reversion
of their exact guard, test and `package.json` registrations. No database, API or
runtime state requires rollback.

## Handoff boundary

Approval of Slice 7 approves only this unresolved placeholder foundation.
Slice 8 and any prerequisite satisfaction, real identity, assignment,
relationship, conflict, disclosure, recusal, waiver, decision, approval,
activation or readiness change require separate user authorization.
